const express=require("express"),Datastore=require("nedb"),app=express(),user_db=new Datastore("user_coords.db"),port=process.env.PORT||3000;app.listen(port,()=>console.log("Server started listening at 3000")),app.use(express.static("static_files")),app.use(express.json({limit:"1mb"})),user_db.loadDatabase(),app.post("/user_geo_coords",(request,response)=>{const response_data=request.body;console.log(response_data),user_db.insert(response_data)});